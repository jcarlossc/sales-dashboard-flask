[project]
name = "sales-dashboard-flask"
version = "0.1.0"
description = ""
authors = [
    {name = "jcarlossc",email = "jcarlossc1977@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask (>=3.1.3,<4.0.0)"
]

[tool.poetry.scripts]
sales = "sales_dashboard_flask.app.app:run"

[tool.poetry]
packages = [{include = "sales_dashboard_flask", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
